# Core
torch
torchvision
numpy
pillow
pyyaml
tqdm
matplotlib
opencv-python

# Classification (RobustBench + paper classifiers)
robustbench @ git+https://github.com/RobustBench/robustbench.git
timm

# VLM (LLaVA-based LVLM pipelines)
transformers
accelerate
sentencepiece
protobuf
einops
open_clip_torch

# Purification (diffusion-based defenses)
torchsde
lmdb
ninja

# Optional: DiffAttack / DiffHammer / DiffBreak baselines (install separately if needed)

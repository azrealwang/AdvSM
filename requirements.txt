# Core
torch
torchvision
numpy
pillow
pyyaml
tqdm
matplotlib
opencv-python

# Classification (install RobustBench after LLaVA — see README Install)
timm>=1.0.9

# VLM (LLaVA-based LVLM pipelines)
transformers
accelerate
sentencepiece
protobuf
einops
open_clip_torch

# Purification (diffusion-based defenses)
torchsde
lmdb
ninja

# Optional: DiffAttack / DiffHammer / DiffBreak baselines (install separately if needed)
